cmake_minimum_required(VERSION 3.18)
set(proj_name soft_body_fem)
project(${proj_name})
include(${PROJECT_SOURCE_DIR}/../../script/functions.cmake)
init_project(${PROJECT_SOURCE_DIR}/../../../simplex)
use_ext_libs(LINK "cuda;openmp;physbam;tiny_obj_loader;tri2d;tetgen")
ignore_src_modules("parallel;")
use_src_libs(SELF "physics;mesh_ext")
setup_exe_target()